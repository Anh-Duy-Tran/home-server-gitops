apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: mirotalk

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml

labels:
  - pairs:
      app.kubernetes.io/name: mirotalk
      app.kubernetes.io/part-of: mirotalk
