apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: prefect

resources:
  - namespace.yaml
  - prefect-server.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: prefect
      managed-by: argocd
